# Local dev (optional): a local Postgres.
# For cloud, set DATABASE_URL to your Neon connection string in the host dashboard.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/timetable
# PORT is provided automatically by Render; locally it defaults to 4100.
